# Git-annex from scratch

I found I missed an under-the-hood explanation of what git-annex added on top of an ordinary git repository.  Here goes:

```{runblock} console
$ ls working
```

```{runblock} console
$ cd working
```

```{runblock} console
$ mkdir my-repo
```

```{runblock} console
$ cd my-repo
```

```{runblock} console
$ git init
$ ls .git
```
